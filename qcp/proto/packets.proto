syntax = "proto3";

package packets;

// Packet 1
message Heartbeat {
    uint32 uptime = 1;
}

// Packet 2
message Request {
    repeated uint32 packet_ids = 1;
}

// Packet 3
message GNSS {
    float latitude = 1;
    float longitude = 2;
    float altitude = 3;
}
